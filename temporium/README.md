# ⏳ Temporium - СУБД "Компьютерные Игры"

## Курсовая работа по дисциплине "Программирование"

**ФГБОУ ВО «Новосибирский государственный технический университет»**  
Кафедра «Защита информации»

---

## 📋 Описание

Temporium — десктопное приложение для управления базой данных компьютерных игр с графическим интерфейсом. Разработано на C++ с использованием Qt5 для GUI и PostgreSQL для хранения данных.

### Структура записи:
- Название игры
- Место на диске (0.1 - 500 ГБ)
- Потребление оперативной памяти (0.5 - 128 ГБ)
- Требуемая видеопамять (0.5 - 48 ГБ)
- Жанр игры
- Статус прохождения (да/нет)
- **Ссылка на игру** (Steam, GOG и т.д.)

---

## 🚀 Быстрый старт

```bash
# 1. Установка зависимостей (один раз)
./run.sh install

# 2. Сборка приложения
./run.sh build

# 3. Запуск (БД + приложение)
./run.sh run

# 4. (Опционально) Установка ярлыка на рабочий стол
./run.sh desktop
```

---

## 📖 Все команды

| Команда | Описание |
|---------|----------|
| `./run.sh install` | Установить зависимости (требуется sudo) |
| `./run.sh build` | Собрать приложение + создать ярлык |
| `./run.sh run` | Запустить всё (БД + GUI) |
| `./run.sh app` | Запустить только GUI |
| `./run.sh db-start` | Запустить только PostgreSQL |
| `./run.sh db-stop` | Остановить PostgreSQL |
| `./run.sh db-shell` | Подключиться к БД |
| `./run.sh desktop` | Пересоздать ярлык на рабочем столе |
| `./run.sh reset-admin` | Сбросить админа к admin/admin123 |
| `./run.sh deb` | Создать DEB-пакет для установки |
| `./run.sh clean` | Очистить сборку и данные |

---

## 📦 Создание DEB-пакета

Для создания DEB-пакета (для установки на других машинах):

```bash
# 1. Собрать приложение
./run.sh build

# 2. Создать DEB-пакет
./run.sh deb
```

После успешного выполнения будет создан файл `temporium_2.0.0_amd64.deb`.

### Установка DEB-пакета:

```bash
sudo dpkg -i temporium_2.0.0_amd64.deb
sudo apt install -f  # установка зависимостей
```

После установки приложение появится в меню и может быть запущено командой `temporium`.

### Удаление:

```bash
sudo dpkg -r temporium
```

---

## 👑 Администратор

При первом запуске создаётся администратор по умолчанию:
- **Логин:** `admin`
- **Пароль:** `admin123`

> ⚠️ Рекомендуется изменить логин и пароль администратора после первого входа!

Администратор может:
- Просматривать всех зарегистрированных пользователей
- Удалять пользователей (вместе с их играми)
- Видеть количество игр у каждого пользователя
- **Изменять свой логин** (требуется текущий пароль)
- **Изменять свой пароль**
- **Сбросить учётные данные** к admin/admin123

---

## ✨ Функциональные возможности

1. ✅ Создание файла с заданной структурой записи
2. ✅ Отображение содержимого файла на экране
3. ✅ Комбинируемые фильтры:
   - По статусу прохождения
   - По жанру
   - По занимаемому месту на диске
   - По потреблению ОЗУ
   - По требуемой видеопамяти
   - **По тегам**
   - **По избранному**
   - **По установленным играм**
   - **По наличию оценки**
4. ✅ Экспорт данных в бинарный файл
5. ✅ Просмотр экспортированного файла
6. ✅ Добавление записей с URL-ссылками
7. ✅ Удаление записей по названию
8. ✅ Редактирование любой характеристики
9. ✅ Многопользовательская авторизация
10. ✅ Администрирование пользователей
11. ✅ Ярлык на рабочем столе (создаётся автоматически)
12. ✅ Создание DEB-пакета для установки
13. ✅ **Пользовательские теги** - создание и фильтрация по тегам
14. ✅ **Оценка игр (0-10)** - с цветовой индикацией
15. ✅ **Избранное** - золотые звёзды и подсветка
16. ✅ **Установленные игры** - отслеживание установки и занятого места
17. ✅ **Заметки к играм** - раскрывающаяся панель заметок
18. ✅ **Расширенная статистика** - избранное, пройдено, без оценки, установлено, без ссылки

---

## 🎨 Интерфейс

- **Тёмная тема** с акцентным цветом #03fce8 (бирюзовый)
- **Запоминание пользователя** между сессиями
- **Навигация стрелками** на экране входа
- **Кликабельные ссылки** на игры (открываются в браузере одним кликом)
- **Toggle selection** — повторный клик снимает выделение
- **Изменяемая ширина столбцов** — столбцы можно перетаскивать
- **Кнопка "Обновить"** также сбрасывает ширину столбцов по умолчанию
- **Меню администратора** скрыто для обычных пользователей
- **Раскрывающаяся панель заметок** — можно редактировать заметки без открытия диалога
- **Статусная панель** с расширенной статистикой коллекции

---

## 🔒 Безопасность

### Защита от SQL-инъекций
Все запросы используют **параметризованные запросы**:
```cpp
txn.exec_params(
    "INSERT INTO users (username, password_hash) VALUES ($1, $2)",
    username, password_hash
);
```

### Хэширование паролей
- Алгоритм: **SHA-256** с солью

### Проверка целостности файлов
- Магическое число для идентификации формата
- Версия формата
- Контрольная сумма SHA-256

### Изоляция данных
Каждый пользователь видит только свои игры.

---

## 🛠 Требования

### Системные:
- Ubuntu 22.04+ / Linux
- Docker и Docker Compose

### Зависимости:
- CMake 3.16+
- Qt5 (qtbase5-dev)
- libpqxx-dev
- libssl-dev
- libpq-dev

---

## 📁 Структура проекта

```
temporium/
├── include/                # Заголовочные файлы
│   ├── types.h             # Структуры данных, константы
│   ├── hash_utils.h        # Хэширование SHA-256
│   ├── database_manager.h
│   └── mainwindow.h        # GUI
├── src/                    # Исходный код
│   ├── main.cpp
│   ├── mainwindow.cpp
│   └── database_manager.cpp
├── sql/
│   └── init.sql            # Инициализация БД
├── docker/
│   └── docker-compose.yml  # PostgreSQL
├── build/                  # Сборка (создается)
│   └── Temporium           # Исполняемый файл
├── CMakeLists.txt
├── run.sh                  # Скрипт управления
├── install-desktop.sh      # Установка ярлыка
└── README.md
```

---

## 🎮 Доступные жанры

Action, Adventure, RPG, Strategy, Simulation, Sports, Racing, Puzzle, Horror, Shooter, Fighting, Platformer, Sandbox, MMO, Other

---

## 📝 Бинарный формат файла (v2)

### Заголовок (100 байт)
```c++
struct BinaryFileHeader {
    uint32_t magic;       // 0x54454D50 ("TEMP")
    uint16_t version;     // Версия формата (2)
    uint32_t record_count;// Количество записей
    char hash[64];        // SHA-256 хеш данных
    uint8_t reserved[26]; // Резерв
};
```

### Запись игры
```c++
struct BinaryGameRecord {
    int32_t id;
    char name[256];
    double disk_space;
    double ram_usage;
    double vram_required;
    char genre[64];
    uint8_t completed;
    char url[512];        // URL ссылка (новое)
    int32_t user_id;
};
```

---

## 🔧 Переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| DB_HOST | localhost | Хост PostgreSQL |
| DB_PORT | 5432 | Порт PostgreSQL |
| DB_NAME | gamedb | Имя базы данных |
| DB_USER | postgres | Пользователь БД |
| DB_PASSWORD | postgres | Пароль БД |

---

## 👨‍💻 Автор

Студент группы АБ-422 Тюриков Максим Олегович  
Кафедра «Защита информации»  
2025 год
